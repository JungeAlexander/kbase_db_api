Generic single-database configuration.

# Runs

```
alembic revision --autogenerate -m "Initial migration"
alembic revision --autogenerate -m "Add entities and entity_mentions"
```

To upgrade: `alembic upgrade head`

Note: config assumes running alembic from outside docker container.
