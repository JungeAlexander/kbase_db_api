{
    "configurations": [
        {
            "name": "Python: fastAPI",
            "type": "python",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}/source",
            "env": {
                "PYTHONPATH": "${workspaceFolder}/source"
            }
        }
    ]
}
